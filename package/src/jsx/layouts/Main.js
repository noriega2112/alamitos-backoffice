import React from 'react'

const Main = ({ children }) => {
    //console.log('render Main')
    return (
        <div>
            {children}
        </div>
    )
}

export default Main;
